# Benchmarks

This directory documents the public benchmark suite used to compare strategy conclusions across engines.

The goal is not to prove that every engine has the same API. The goal is to check whether independent engines lead to the same investment conclusion when data, assumptions, rebalance timing, cost model, and strategy logic are aligned.
